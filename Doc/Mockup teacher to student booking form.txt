Found it — **line 674**: the `+ Book` button itself turns into `Close` when the form is open. So there are two Close buttons at once: the card toggle button **and** the one inside the form. The card button should stay as `+ Book` / `− Hide` rather than switching to `Close`.

Aktualisiertes Schema:

```
┌──────────────────────────────────────────┐
│ .slot-card  (Buchungen tab)              │
│                                          │
│  09:00 – 09:30   Available   [ − Hide ]  │  ← toggles form open/closed
│                                          │
│ ┌────────────────────────────────────┐   │
│ │ .booking-form                      │   │
│ │                                    │   │
│ │  STUDENTS                          │   │
│ │  ☐ Anna Müller                     │   │
│ │  ☐ Ben Schmidt                     │   │
│ │                                    │   │
│ │  ┌────────────┐  ┌──────────────┐  │   │
│ │  │ FROM       │  │ UNTIL        │  │   │
│ │  │ [09:00 ▾]  │  │ [09:30 ▾]   │  │   │
│ │  └────────────┘  └──────────────┘  │   │
│ │                                    │   │
│ │  [ Book ]  [ Close ]               │   │  ← only 1 Close now
│ └────────────────────────────────────┘   │
└──────────────────────────────────────────┘
```

- **`− Hide`** — oben rechts auf der Karte, klappt das Formular zu
- **`Close`** — im Formular selbst, macht dasselbe
- Kein doppeltes `Close` mehr


Understood. Here's what I'll do:

1. **Remove** the `available` status label from the booking card
2. **Collapsed**: time on left, `[+ Book]` / `[Cancel]` on the right (old row layout)
3. **Expanded**: time + status info on left, button moves **below** into the form area (current layout)

Now fix the CSS — `slot-card-top-row` back to row layout:

Schema:

```
── COLLAPSED ──────────────────────────────────
│ .slot-card-top-row                          │
│                                             │
│  09:00 – 09:30              [ + Book ]      │
│  (left)                     (right)         │
└─────────────────────────────────────────────┘

── EXPANDED ───────────────────────────────────
│ .slot-card-top-row                          │
│                                             │
│  09:00 – 09:30              [ − Hide ]      │
│                                             │
│ ┌─────────────────────────────────────────┐ │
│ │ .booking-form                           │ │
│ │                                         │ │
│ │  STUDENTS                               │ │
│ │  ☐ Anna Müller                          │ │
│ │  ☐ Ben Schmidt                          │ │
│ │                                         │ │
│ │  ┌──────────┐  ┌──────────┐             │ │
│ │  │ FROM     │  │ UNTIL    │             │ │
│ │  │ [09:00▾] │  │ [09:30▾] │             │ │
│ │  └──────────┘  └──────────┘             │ │
│ │                                         │ │
│ │  [ Book ]  [ Close ]                    │ │
│ └─────────────────────────────────────────┘ │
└─────────────────────────────────────────────┘

── BOOKED ─────────────────────────────────────
│  10:00 – 10:30              [ Cancel ]      │
│  Anna Müller (S1)                           │
└─────────────────────────────────────────────┘
```

- **Collapsed**: Zeit links, Button rechts — kompakt
- **Expanded**: Form öffnet sich darunter mit Students, From/Until, Book + Close
- **"available" Label**: entfernt